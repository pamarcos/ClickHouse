version: '2.3'
services:
    local_kms:
        image: nsmithuk/local-kms:3.12.0
        restart: always
        environment:
            PORT: ${LOCAL_KMS_PORT}
        ports:
            - ${LOCAL_KMS_PORT}:${LOCAL_KMS_PORT}
